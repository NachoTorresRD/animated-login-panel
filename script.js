"use strict";(()=>{const tabs=[...document.querySelectorAll('[role="tab"]')],panels=[...document.querySelectorAll('[role="tabpanel"]')],forms=[...document.querySelectorAll("form")],toggles=[...document.querySelectorAll("[data-password]")],status=document.querySelector("#form-status");if(!tabs.length||!panels.length||!forms.length||!status){console.warn("Animated Login Panel: DOM incompleto.");return}const activate=tab=>{tabs.forEach(item=>{const active=item===tab;item.setAttribute("aria-selected",String(active));item.tabIndex=active?0:-1});panels.forEach(panel=>panel.hidden=panel.id!==tab.getAttribute("aria-controls"));status.textContent=""};tabs.forEach((tab,index)=>{tab.addEventListener("click",()=>activate(tab));tab.addEventListener("keydown",event=>{if(!["ArrowLeft","ArrowRight"].includes(event.key))return;event.preventDefault();const next=event.key==="ArrowRight"?(index+1)%tabs.length:(index-1+tabs.length)%tabs.length;activate(tabs[next]);tabs[next].focus()})});toggles.forEach(toggle=>toggle.addEventListener("click",()=>{const input=document.getElementById(toggle.dataset.password);if(!input)return;const visible=input.type==="text";input.type=visible?"password":"text";toggle.textContent=visible?"Mostrar":"Ocultar";toggle.setAttribute("aria-label",visible?"Mostrar contraseña":"Ocultar contraseña")}));const message=input=>{if(input.validity.valueMissing)return"Este campo es obligatorio.";if(input.validity.typeMismatch)return"Escribe un correo válido.";if(input.validity.tooShort)return`Usa al menos ${input.minLength} caracteres.`;return""};forms.forEach(form=>form.addEventListener("submit",event=>{event.preventDefault();let firstInvalid=null;[...form.elements].filter(el=>el instanceof HTMLInputElement).forEach(input=>{const error=document.getElementById(`${input.id}-error`),text=message(input);input.setAttribute("aria-invalid",String(Boolean(text)));if(error)error.textContent=text;if(text&&!firstInvalid)firstInvalid=input});if(firstInvalid){status.textContent="Revisa los campos señalados.";firstInvalid.focus()}else{status.textContent="Validación correcta. Esta demo no envió ni guardó tus datos.";form.reset()}}))})();
